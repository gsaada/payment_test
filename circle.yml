machine:
  services:
    - docker
    - sudo mv ./payment_test/circle-docker.sh ../
    - sudo chmod 0755 circle-docker.sh

dependencies:
  override:
    - circle-docker.sh env
    - circle-docker.sh build rollout/dsymprocessor

test:
  post:
          - circle-docker.sh run rollout/dsyprocessor

deployment:
  hub:
    branch: master
    commands:
            - circle-docker.sh push rollout/dsymprocessor
