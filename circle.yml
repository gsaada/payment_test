machine:
  services:
    - docker

dependencies:
  override:
    - circle-docker env
    - circle-docker build rollout/dsymprocessor

test:
  post:
          - circle-docker run rollout/dsyprocessor

deployment:
  hub:
    branch: master
    commands:
            - circle-docker push rollout/dsymprocessor
